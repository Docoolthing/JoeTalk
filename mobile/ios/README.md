This directory is reserved for the Flutter iOS project scaffold.

Generate the full iOS host project locally with:

```
flutter create --platforms=android,ios --project-name joe_talk_mobile --overwrite .
```

Run the command from the `mobile/` directory after installing Flutter SDK.
